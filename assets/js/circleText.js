/* const text = document.querySelector('.join__description');

// Подождись, пока DOM загрузится
window.addEventListener('DOMContentLoaded', () => {
  if (window.innerWidth > 766) {
    const circleType = new CircleType(text);
    circleType.dir(-1).radius(200);
  }
}); */